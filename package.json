const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req, res) => {
    res.send(`
        <h1>Welcome to Elevate Lab 🚀</h1>
        <p>This app is created by <strong>Mohammed Faisal Iqbal</strong></p>
        <p>I’m doing a 1-month internship here and learning DevOps and CI/CD with Jenkins!</p>
    `);
});

app.listen(PORT, () => {
    console.log(`Server running on http://localhost:${PORT}`);
});
